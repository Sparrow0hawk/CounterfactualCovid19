version: "3"

services:
  backend:
    build: ./backend
    environment:
      - DJANGO_SETTINGS_MODULE=backend.settings.docker
    expose:
      - "8000"
    restart: always
  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "80:80"
    restart: always
